spring:
  application:
    name: be-batch
  datasource:
    url: jdbc:h2:mem:batch;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  batch:
    jdbc:
      initialize-schema: always
#    job:
#      enabled: false
