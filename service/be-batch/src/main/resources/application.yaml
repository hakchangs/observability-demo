spring:
  application:
    name: be-batch
  datasource:
    url: jdbc:h2:mem:batch;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  batch:
    job:
      enabled: false        # K8s Job args 로 job name 지정 시에만 실행
    jdbc:
      initialize-schema: always
