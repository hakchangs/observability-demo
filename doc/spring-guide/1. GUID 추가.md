# GUID 추가: Spring Guide

필요작업
1. GUID 획득 및 생성: baggage > guid 추출. 없다면, 생성
2. 전파 설정: baggage > guid 설정 (기존/신규 무관)
3. Span 저장: guid span 저장 (검색조건 사용가능하도록 추가)

### Filter 추가 예제

##### 1. 라이브러리 추가
```xml
<!-- OTEL Custom 을 위한 기본 LIB -->
<dependency>
    <groupId>io.opentelemetry</groupId>
    <artifactId>opentelemetry-api</artifactId>
    <version>1.60.1</version>
</dependency>
```

##### 2.
```java

```

